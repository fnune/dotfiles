#!/usr/bin/env bash

DIRECTORY="$HOME/.tmux/plugins/tmp"
REPOSITORY_URL="https://github.com/tmux-plugins/tpm"

if [ ! -d "$DIRECTORY" ]; then
  git clone $REPOSITORY_URL $DIRECTORY
else
  pushd "$DIRECTORY"
    git pull $REPOSITORY_URL
  popd
fi

